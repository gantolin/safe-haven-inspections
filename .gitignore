# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output

# Local env files (.env.example is committed on purpose)
.env
.env.*
!.env.example
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results/
playwright-report/

# Client work product. This repo is PUBLIC — inspection reports carry the
# property address, lab results, and interior photographs of someone's home,
# and must never be committed. Badge artwork extracted from a report belongs
# in public/ as a standalone asset; the report itself does not belong here.
*.pdf
/IMG_*.JPG
/IMG_*.jpg
